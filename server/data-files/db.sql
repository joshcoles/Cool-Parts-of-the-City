
CREATE TABLE users (
  id BIGSERIAL PRIMARY KEY,
  account VARCHAR(50),
  email VARCHAR(50),
  password VARCHAR(255)
  );

CREATE TABLE maps (
  id BIGSERIAL PRIMARY KEY,

);

CREATE TABLE coordinates (
  id BIGSERIAL PRIMARY KEY,

);

CREATE TABLE favourites (
  id BIGSERIAL PRIMARY KEY,

);

